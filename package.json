{
  "name": "trustchain",
  "version": "0.1.0",
  "description": "Blockchain-based platform for decentralized identity, RBAC access control, and NFT-based digital asset management",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "seed:local": "hardhat run scripts/seed-local.js --network localhost",
    "web": "node scripts/serve.js",
    "demo": "hardhat run scripts/demo.js",
    "accounts": "hardhat run scripts/accounts.js",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "wallet:new": "hardhat run scripts/wallet-new.js",
    "preflight": "hardhat run scripts/preflight.js --network sepolia",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify:sepolia": "hardhat run scripts/verify.js --network sepolia",
    "bundle": "node scripts/bundle-console.js",
    "chain": "node scripts/chain.js",
    "chain:audit": "node scripts/chain.js audit 20",
    "chain:blocks": "node scripts/chain.js blocks",
    "chain:verify": "node scripts/chain.js verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.26.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^17.4.2"
  }
}
